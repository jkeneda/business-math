/* window.MathJax = {
    tex: {
      tags: 'ams'
    }
  };

  Used for the auto-numbering attempt. May return later.
  Need to uncomment the corresponding script tags at the bottom of _layouts/default.html to re-enable. */